require("bufferline").setup{
    options = {
        numbers = "buffer_id",
        indicator = {
            style = 'none'
        },
        show_buffer_close_icons = false,
        always_show_bufferline = false
    }
}

