# The Simple X Hotkey Daemon is the program used by LARBS to map most of the
# dwm-independent commands.  This file is pretty readable, so I'll let you read
# it.

# The keys NOT bound here by sxhkd, but by dwm or i3 are:

# Basic binds
super + Return
	$TERMINAL
super + r
	rofi -show drun
super + f
	firefox
super + n
    nautilus


super + shift + x
	dprompt "Shutdown computer?" "sudo -A shutdown -h now"
super + shift + r
    dprompt "Restart computer?" "sudo -A shutdown -r now"
super + shift + BackSpace
    i3reboot
alt + shift + l
    i3lock-script now


control + shift + l
    xkblayout-state set +1; dwmbar update keyboard &


XF86PowerOff
	dprompt "Shutdown computer?" "sudo -A shutdown -h now"
XF86AudioRaiseVolume
    volumectl up 5 ; dwmbar update volume &
XF86AudioLowerVolume
    volumectl down 5;  dwmbar update volume &
XF86AudioMute
    volumectl mute; dwmbar update volume &

alt + Up
    volumectl up 5 ; dwmbar update volume &

alt + Down
    volumectl down 5 ; dwmbar update volume &


# bindsym $mod+Shift+p exec playerctl --player=spotify play-pause
# bindsym $mod+Right exec playerctl --player=spotify next
# bindsym $mod+Left exec playerctl --player=spotify previous
#
# bindsym $mod+Up $volume up 2
# bindsym $mod+Down $volume down 2


